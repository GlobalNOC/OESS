<label-switched-path>
  <name>OESS-L2VPN-[% PATH_NAME %]-LSP-[% circuit_id %]</name>
  <apply-groups>L2VPN-LSP-ATTRIBUTES</apply-groups>
  <to>[% destination_ip %]</to>
  <primary>
    <name>OESS-L2VPN-[% PATH_NAME %]-PATH-[% circuit_id %]</name>
    [% IF PATH_NAME == 'primary' %]
    <select>manual</select>
    [% ELSE %}
    <standby />
    [% END %]
  </primary>
</label-switched-path>
<path>
  <name>OESS-L2VPN-[% PATH_NAME %]-PATH-[% circuit_id %]</name>
  <path-list>
    <name>[% destination_ip %]</name>
    [% IF path_type == 'explicit' %]
      [% FOREACH ip in PATH %]
    <path-list>
      <name>[% ip %]</name>
      <strict/>
    </path-list>
      [% END %]
    [% ELSE %]
    <loose/>
    [% END %]
  </path-list>
</path>
